import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com', 'img.freepik.com', 'another-domain.com'], // Add all your domains here
  },
};

export default nextConfig;
