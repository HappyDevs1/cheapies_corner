import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com', 'img.freepik.com', 'cdn.yello.link'], // Add all your domains here
  },
};

export default nextConfig;
